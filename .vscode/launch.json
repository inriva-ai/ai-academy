{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python Debugger: Debug MetaFlow Run",
            "type": "debugpy",
            "request": "launch",
            "program": "${file}",
            "console": "integratedTerminal",
            "cwd": "${fileDirname}",
            "args": ["run"]
        }
    ]
}